{
  "commands": [
    {
      "type": "regex",
      "phrases": [
        ".*(ram|memory).*(usage|status|much|utilization|consumption).*",
        ".*(how is|tell me).*(ram|memory).*"
      ],
      "action": "get_system_stats",
      "params": {}
    },
    {
      "type": "regex",
      "phrases": [
        ".*(cpu|processor).*(usage|status|load|performance|utilization).*",
        ".*(how is|tell me).*(cpu|processor).*"
      ],
      "action": "get_system_stats",
      "params": {}
    },
    {
      "type": "regex",
      "phrases": [
        ".*(battery|power).*(status|level|percentage|health|life|remaining|charge).*",
        ".*(how much|tell me).*(battery|power).*"
      ],
      "action": "get_system_stats",
      "params": {}
    },
    {
      "type": "regex",
      "phrases": [
        ".*(system|pc|computer|hardware).*(status|info|overview|specs|summary|specifications).*",
        ".*(tell me about).*(system|pc|computer|hardware).*"
      ],
      "action": "get_system_stats",
      "params": {}
    },
    {
      "type": "regex",
      "phrases": [
        ".*(take|capture|grab|do|print)?.*(screenshot|desktop).*",
        ".*(take|capture|grab|do|print).*(screen).*"
      ],
      "action": "take_screenshot",
      "params": {}
    },
    {
      "type": "regex",
      "phrases": [
        ".*(take|capture|click|grab).*(photo|picture|camera|webcam).*"
      ],
      "action": "capture_camera",
      "params": {}
    },
    {
      "type": "regex",
      "phrases": [
        ".*(set|change|adjust).*(bright).*(to)? (\\d+).*",
        ".*(bright).*(to)? (\\d+).*"
      ],
      "action": "set_brightness_level",
      "params": {}
    },
    {
      "type": "regex",
      "phrases": [
        ".*(clean|clear|free|optimize|boost).*(pc|ram|memory).*"
      ],
      "action": "clean_memory",
      "params": {}
    },
    {
      "type": "regex",
      "phrases": [
        ".*(turn off|disable|sleep).*(screen|display|monitor).*"
      ],
      "action": "run_cmd",
      "params": { "command": "powershell -Command \"(Add-Type '[DllImport(\\\"user32.dll\\\")]public static extern int SendMessage(int h,int m,int w,int l);' -Name a -Pas)::SendMessage(-1,0x0112,0xF170,2)\"" }
    }
  ]
}
